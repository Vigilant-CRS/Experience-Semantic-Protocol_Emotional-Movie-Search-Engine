# MindRead V3 — Python 3.12
# Last refresh: 2026-05-12 (audit F-009: phantom deps removed,
# llama-cpp-python added — it was imported but not declared)

# Core
requests==2.32.3
numpy<2                          # pinned for sklearn 1.4 compat (audit 2026-05)

# Embeddings (E5-large-v2)
sentence-transformers==3.0.1
transformers==4.41.2
torch>=2.2.0
scikit-learn==1.4.0
huggingface-hub>=0.23.0
accelerate==0.31.0
safetensors==0.4.3

# Vector store
qdrant-client==1.9.1

# Local LLM batch path (Qwen via GGUF)
llama-cpp-python>=0.3.21

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# Progress bars (used internally by sentence-transformers + qdrant)
tqdm==4.66.4

# --- dev (optional) ---
pytest==8.2.2
black==24.3.0
ruff==0.4.1
